import React from "react";

// By email
// NewsLetter preferance (checkbox): daily, weekly, monthly
// Push notification (radio): everything, new content added, new comments

function Notification() {
  return (
    <div>
      <h1>Notification page is being rendered.</h1>
    </div>
  );
}

export default Notification;
